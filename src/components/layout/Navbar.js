import { Navbar } from 'react-bootstrap';

const Navmenu = () => (
  <Navbar>
    <Navbar.Header>
      <Navbar.Brand>
        <a href="#">Michael Humiston</a>
      </Navbar.Brand>
      <Navbar.Toggle />
    </Navbar.Header>
  </Navbar>
);

export default Navmenu;
